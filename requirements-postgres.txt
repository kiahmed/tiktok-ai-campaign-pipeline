# Optional: PostgreSQL support (the SQLite -> Postgres upgrade path).
#   pip install -r requirements.txt -r requirements-postgres.txt
# then set DATABASE_URL=postgresql+psycopg2://user:pass@host:5432/dbname
psycopg2-binary==2.9.10
